# Development & test dependencies (installed on top of requirements.txt).

-r requirements.txt

pytest>=8.3.0,<9.0.0
httpx>=0.27.0,<1.0.0          # ASGI test client for FastAPI
ruff>=0.6.0,<1.0.0            # linter + formatter
mypy>=1.11.0,<2.0.0           # static type checking
fakeredis>=2.20.0,<3.0.0      # in-memory Redis for testing app.core.redis-backed code, hermetically
